class Solution {
public:
    int maxSubArray(vector<int>& nums) {

        int currentSum = nums[0];
        int maxSum = nums[0];

        for (int i = 1; i < nums.size(); i++) {

            // Either start a new subarray
            // or add the current number
            currentSum = max(nums[i], currentSum + nums[i]);

            // Update maximum sum
            maxSum = max(maxSum, currentSum);
        }

        return maxSum;
    }
};
